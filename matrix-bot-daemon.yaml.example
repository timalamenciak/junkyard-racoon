# Copy this file to matrix-bot-daemon.yaml and edit as needed.
#
# LLM settings are loaded automatically from power-tools/configs/llm.yaml —
# no duplication needed here.
#
# Use the commands section to enable/disable plug-in tool scripts and pass
# any extra environment variables they need (e.g. API tokens for external services).

commands:
  # example_tool:
  #   enabled: true
  #   env:
  #     SOME_API_TOKEN: replace-me
